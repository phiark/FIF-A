# Auto detect text files and perform LF normalization
* text=auto

# Normalize line endings for scripts and code
*.py text eol=lf
*.sh text eol=lf

# Treat common binaries as binary (no diffs)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.pt filter=lfs diff=lfs merge=lfs -text
